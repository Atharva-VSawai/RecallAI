# Appendix

The current paper keeps the reproducibility protocol in the main text. This directory is reserved for a future corpus manifest, annotation schema, and raw evaluation-trace format once a controlled benchmark is released.
