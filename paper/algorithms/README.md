# Algorithms

The algorithm listings are embedded in `main.tex` so that IEEEtran can float and cross-reference them consistently. The source-level pseudocode is derived from `backend/ingestion/pipeline.py`, `backend/agents/router.py`, `backend/agents/query_agent.py`, and `backend/agents/impact_agent.py`.
